diff --git a/tui_gateway/server.py b/tui_gateway/server.py
index ae5d5857..b152ef02 100644
--- a/tui_gateway/server.py
+++ b/tui_gateway/server.py
@@ -4518,7 +4518,7 @@ def _(rid, params: dict) -> dict:
         from tools.cronjob_tools import cronjob
 
         if action == "list":
-            return _ok(rid, json.loads(cronjob(action="list")))
+            return _ok(rid, json.loads(cronjob(action="list", include_disabled=True)))
         if action == "add":
             return _ok(
                 rid,
@@ -4531,7 +4531,7 @@ def _(rid, params: dict) -> dict:
                     )
                 ),
             )
-        if action in ("remove", "pause", "resume"):
+        if action in ("remove", "pause", "resume", "run"):
             return _ok(rid, json.loads(cronjob(action=action, job_id=jid)))
         return _err(rid, 4016, f"unknown cron action: {action}")
     except Exception as e:
